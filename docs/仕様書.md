# web-launcher 仕様書
 
## 1. 概要 / 目的
- web-launcher は、ローカルの `user_data` ディレクトリ内にある CSV ファイル群を使って URL リスト（ブックマーク）を管理・表示・編集し、ブラウザで各 URL をワンクリックで開ける Streamlit アプリケーションです。
- 主な用途：個人のブックマーク管理、チームでの URL 共有、カテゴリや頻度別の管理。

## 2. 実行要件 / 依存関係
- 必須パッケージ（推奨インストール方法は `requirements.txt` を利用）:
  - streamlit >= 1.22.0
  - pandas >= 1.5.0
- 開発用ツール:
  - ruff（`pyproject.toml` に設定あり）
- 起動コマンド（作業ディレクトリをリポジトリルートにて）:

- 起動手順: 詳細は `README.md` の「インストールと起動」セクションを参照してください。

## 2.1 動作環境
- OS: Windows 10/11（主要な開発・動作確認環境）
- Python: 3.8以上（type hints対応）
- ブラウザ: Chrome/Edge/Firefox 最新版（`webbrowser.open`で開く際の想定ブラウザ）
- ディスク容量: 100MB以上（仮想環境、依存パッケージを含む）

## 3. ディレクトリ構成（重要ファイル）
- `app.py` — メインページ（表示用）
- `streamlit_utils.py` — ユーティリティ群（CSV読み込み、保存、URLを開く、リンクボタン作成）
- `pages/02_編集.py` — 編集ページ（CSV の編集・新規作成・アップロード）
- `pyproject.toml` — ruff 設定など
- `README.md` — 使用方法・概要
- `user_data/` — CSV を配置するフォルダ（例: 01_daily.csv など）。存在しない場合はアプリが作成を試みる。

## 4. 主な機能（ユーザ視点）
- サイドバーで CSV ファイルを選択して表示（`app.py`）
- 表示した CSV 中の各行に「リンクボタン」を表示し、クリックでブラウザを開く（`streamlit_utils.create_link_button`）
- 「全て開く」ボタンで、表示中のタブにある全 URL を一括で開く（`streamlit_utils.open_urls`）
- 編集ページ（`pages/02_編集.py`）：
  - CSV の行編集（`st.data_editor`）
  - 保存（原子的に一時ファイル経由で上書き：`save_dataframe`）
  - 新規 CSV 作成（`NAME`, `URL` の2列を初期化）
  - CSV のアップロード（アップロード後保存）
  - 保存時はセッションにトーストメッセージを入れ、ページをリロードしてトースト表示

## 5. データ形式／CSV仕様
- 必要列（想定）：
  - `NAME` — 表示名（省略時は行インデックスを使用）
  - `URL` — 開く URL（空欄可）
- CSV のエンコーディング読み取り：
  - `streamlit_utils._try_read_csv` は順に `utf-8`, `cp932`, `shift_jis`, `utf-16` を試す。
  - 全て失敗した場合はエラーメッセージを表示し、空の DataFrame をリストに追加する。
- 保存は pandas.DataFrame.to_csv で出力（`index=False`）。

## 6. 主要内部設計（関数責務）
- app.py
  - `main()`:
    - `user_data` フォルダを作成（存在しない場合）し、`load_csv_files()` で CSV を読み込む
    - サイドバーでファイル選択（radio）
    - 選択された CSV を表示。`URL` 列があれば各行ごとにリンクボタンを作成、無ければ案内を表示
    - 「全て開く」ボタンで `open_urls()` を呼び出す
- streamlit_utils.py
  - `_try_read_csv(path)`: 複数エンコーディングでの読み取り試行
  - `load_csv_files(path)`: 指定ディレクトリの `*.csv` を取得して DataFrame リストを生成
  - `save_dataframe(df, csv_file)`: 一時ファイル経由で保存（原子的な置換）
  - `open_urls(urls)`: 与えられた URL を順に `webbrowser.open()` で開く
    - 入力を文字列化して前後空白を除去し、None/空文字をスキップします。スキームが無ければ自動的に `http://` を補完します（ただし `mailto:`, `ftp://` など既存の明示的スキームは尊重）
    - エラー時は Streamlit の UI に `st.error` を表示し、st が利用できないコンテキストでは標準出力へフォールバック
  - `create_link_button(row)`: `NAME` と `URL` を使って `st.link_button` を試し、失敗時は Markdown にフォールバック
- pages/02_編集.py
  - `main()`: 編集 UI を表示。`handle_edit_mode`, `handle_create_mode`, `handle_upload_mode` を呼ぶ
  - `handle_edit_mode()`: 各 CSV ごとに `st.data_editor` を使って編集。保存ボタンで `save_dataframe` 呼び出し、成功時は `st.rerun()` して変更を反映
  - `handle_create_mode()`: サイドバーで新規 CSV を作成（`NAME`, `URL` 列を初期化）
  - `handle_upload_mode()`: サイドバーで CSV アップロード受け付け。読み込み→保存

## 7. 画面フロー（ユーザ操作の流れ）
1. `streamlit run app.py` 起動 → メインページ表示（左サイドバーに CSV ファイル名ラジオ）
2. サイドバーでファイル選択 → 選択ファイルの表がメイン領域に表示
   - 各行にリンクボタン（NAME と URL から作成）
   - 全て開くボタンでタブ内全 URL をブラウザで順次開く
3. 画面上部の「編集」ページ（Streamlit pages）に移動すると CSV を直接編集可能
   - 編集 → 保存 → 成功時にトーストとリロード

## 8. エラーハンドリング / 境界条件
- 読み込み失敗時：`st.error` 表示し、該当ファイルは空の DataFrame として扱う（アプリは停止しない）
- 保存失敗時：`save_dataframe` は一時ファイルを削除しエラーメッセージを返す。UI はエラーメッセージ表示。
- ディレクトリが存在しない場合：`load_csv_files` や `main` は `os.makedirs(..., exist_ok=True)` を試み、作成に成功すれば情報を表示する
- 空の URL は `open_urls` でスキップされる
- 保存中やファイルロックの問題：`save_dataframe` は一時ファイル + `os.replace` を使うため一般的な原子置換を行うが、共有ファイルシステムや権限問題は例外として残る

## 10. セキュリティ / プライバシー
- `user_data` は README に「.gitignore で除外」とある想定。個人データはローカルで管理されるべき。
- `webbrowser.open` はローカルでのブラウザ起動のみ。外部にデータを送信するコードは見当たりません。

## 11. 実装上のテクニカルメモ（開発者向け）
- CSV 読み込み順は `sorted(glob.glob(...))` なのでアルファベット順（ファイル名のソート）で表示される。
- `save_dataframe` は `tempfile.mkstemp` で安全に一時ファイルを作成し `os.replace` で置換。Windows 上でのファイルロック回避に配慮している。
- `create_link_button` は `st.link_button` をまず試み、例外が起きたら Markdown にフォールバックする実装で、互換性をある程度配慮している。

## 12. 既知の制約・改善案（優先度順）
1. 単体テストの追加（必須度中）
   - `streamlit_utils._try_read_csv`, `save_dataframe`, `open_urls` のユニットテスト

## 13. 最低限の受け入れ条件（仕様としての完了基準）
- `streamlit run app.py` で起動して、`user_data` にある既存の CSV が左側の選択で選べる。
- 選択した CSV の各行にリンクボタンが表示され、クリックでブラウザが開く（環境で開けること）。
- 編集ページで CSV の編集 → 保存が可能で、保存後にリロードされ変更が反映される。
- エンコーディングの違う CSV を正しく読み込める（少なくとも UTF-8 / CP932 等を試行する）。

## 14. 実行方法（短縮）
1. 仮想環境を用意（推奨）
2. 依存関係をインストール（`requirements.txt` を利用）

```powershell
pip install -r requirements.txt
```

3. アプリを起動

```powershell
streamlit run app.py
```

4. ブラウザで表示される UI を利用（左サイドバーでファイル選択 / 編集は pages の「編集」）

## 15. 推奨するテストケース（ユニット／手動）
- ユニット（自動）
  - `_try_read_csv`：UTF-8, CP932, 不正バイナリのテストファイル
  - `save_dataframe`：書き込み成功、書き込み失敗（権限エラーやロック）時の挙動
  - `create_link_button`：NAME 有り・無し、URL 空・不正な URL
- 手動（E2E）
  - 既存 CSV の表示 → 各リンクが開くこと
  - 編集で新しい行を追加 → 保存 → 再起動して反映
  - アップロードで CSV を追加 → 保存 → サイドバーに表示

---

## 16. 設定
### デフォルト設定
- CSV格納場所: `user_data/`（環境変数で変更可能）
- CSV必須列: `NAME`, `URL`（追加列は任意）
- 対応エンコーディング: UTF-8, CP932, SHIFT-JIS, UTF-16
- ファイルソート: アルファベット順（`sorted(glob.glob())`）

## 17. トラブルシューティング
### よくある問題と解決方法
1. CSVが読み込めない
   - エンコーディングを確認（UTF-8推奨）
   - 必須列（NAME, URL）の存在確認
   - ファイルの破損チェック
   - ログでエンコーディング試行エラーを確認

2. URLが開かない
   - ブラウザのデフォルト設定確認
   - URLのスキーム確認（http://, https://, mailto: など）
   - ブラウザの起動状態確認
   - エラーメッセージの確認（UI または標準出力）

3. 保存できない
   - ファイルの書き込み権限確認
   - ディスク容量確認
   - 他プロセスによるロック確認
   - 一時ファイル作成権限の確認

### エラーメッセージ一覧
- "CSVの読み込みに失敗しました: [ファイル名]" — エンコーディング試行が全て失敗
- "指定されたフォルダが存在しません: [パス]" — ディレクトリ作成失敗
- "URLを開けませんでした: [URL] ([エラー詳細])" — ブラウザ起動失敗
- "保存に失敗しました: [エラー詳細]" — ファイル書き込みエラー

## 18. 開発者ガイド
### 開発環境セットアップ
```powershell
# 仮想環境作成
python -m venv .venv
.\.venv\Scripts\activate

# 依存関係インストール
pip install -r requirements.txt

# 開発用パッケージ追加
pip install pytest ruff
```

### コーディング規約
- PEP 8準拠（ruffで自動チェック）
- docstringはGoogle形式を採用
- 型ヒントの積極的な使用（Python 3.8+）
- エラーメッセージは日本語で統一

### Git管理ルール
- `user_data/*` は `.gitignore` で除外
- コミットメッセージは日本語で記述
- プルリクエストはテンプレートに従って作成

### プルリクエスト提出前のチェックリスト
1. ruffによるコード整形実行
2. 単体テスト実行
3. 手動での動作確認
   - CSV読み込み
   - URL開く
   - 保存機能

---

作成者: 自動生成（仕様書 v1.1.0）
更新日: 2025-11-09
