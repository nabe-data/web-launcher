{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "type": "command",
        "command": "uv run ruff format . && uv run ruff check --fix . && uv run ruff check ."
      }
    ]
  }
}